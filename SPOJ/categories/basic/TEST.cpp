#include <bits/stdc++.h>
#define eps 1e-6
#define inf (ll)1e9

using namespace std;

typedef long long ll;
typedef long double ld;

ll x;

int main()
{
    while (cin>>x)
    {
        if (x==42) break;
        cout<<x<<endl;
    }
    return 0;
}